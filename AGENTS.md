# MasterControl — Agent Guide

The authoritative guide for AI agents working in this repo is
[`CLAUDE.md`](CLAUDE.md). It covers the stack, layer rules, domain
glossary, AI integration rules, code standards, security boundaries,
and dev commands. Read that file first regardless of which agent
runtime you're using.

This file exists as a redirect for tools (e.g. OpenAI Codex CLI) that
look for `AGENTS.md` by convention.
